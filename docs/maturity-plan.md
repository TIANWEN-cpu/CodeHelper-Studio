# CodeHelper 成熟度改进计划

> 基于 2026-06-02 深度审计结果制定，目标：将综合评分从 7.0/10 提升至 8.5+/10。

---

## 一、当前成熟度评分总览

| 维度             | 当前评分   | 等级     | 目标评分   | 差距     |
| ---------------- | ---------- | -------- | ---------- | -------- |
| 代码质量         | 7/10       | 良好     | 9/10       | +2       |
| 测试             | 7/10       | 良好     | 8/10       | +1       |
| 文档             | 8/10       | 优秀     | 9/10       | +1       |
| UX/Polish        | 7/10       | 良好     | 8/10       | +1       |
| 安全             | 8/10       | 优秀     | 9/10       | +1       |
| Build/Release    | 5/10       | 中等     | 8/10       | +3       |
| DX（开发者体验） | 7/10       | 良好     | 9/10       | +2       |
| 架构             | 7/10       | 良好     | 8/10       | +1       |
| **综合**         | **7.0/10** | **良好** | **8.5/10** | **+1.5** |

---

## 二、P0 — 立即实施（本轮可完成）

### P0-1: 补充截图并完善 README

| 字段         | 内容                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 让 README 成为项目的有效"门面"，提升开源项目的第一印象                                                                                                                                                                                                                                                                                                                 |
| **涉及文件** | `README.md`、`docs/`（新增截图文件）                                                                                                                                                                                                                                                                                                                                   |
| **步骤**     | 1. 运行 `npm run dev` 启动应用，分别截取以下页面：<br>&emsp;- 代码编辑器界面（含 Monaco Editor）<br>&emsp;- AI 对话界面（含流式输出）<br>&emsp;- 题库浏览与答题界面<br>&emsp;- 知识库 RAG 检索界面<br>&emsp;- 设置页面（展示主题切换）<br>2. 将截图保存至 `docs/screenshots/` 目录<br>3. 取消 README 中截图注释，替换为实际图片引用<br>4. 为每张截图添加简短的功能说明 |
| **风险**     | 低。仅涉及文档修改，不影响代码逻辑                                                                                                                                                                                                                                                                                                                                     |
| **预期收益** | 显著提升项目可信度和吸引力；让用户在 30 秒内理解应用形态                                                                                                                                                                                                                                                                                                               |

### P0-2: 提取 CONTRIBUTING.md 独立文件

| 字段         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 提供完整的贡献者上手指南，降低新贡献者的参与门槛                                                                                                                                                                                                                                                                                                                                                                                 |
| **涉及文件** | `CONTRIBUTING.md`（新建）、`README.md`（更新链接）                                                                                                                                                                                                                                                                                                                                                                               |
| **步骤**     | 1. 从 README 贡献指南部分提取现有内容至 `CONTRIBUTING.md`<br>2. 补充以下章节：<br>&emsp;- 开发环境搭建（Node.js 版本、编译器依赖、IDE 推荐）<br>&emsp;- 项目架构概览（三进程模型、数据流、模块划分）<br>&emsp;- 调试技巧（main 进程调试、renderer DevTools、IPC 日志）<br>&emsp;- 常见问题 FAQ<br>&emsp;- 新增功能的步骤指南（添加 IPC channel、新增页面等）<br>3. 在 README 中将贡献指南段落替换为指向 `CONTRIBUTING.md` 的链接 |
| **风险**     | 低。纯文档操作                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **预期收益** | 新贡献者能在 15 分钟内完成环境搭建并理解架构，而非面对 30+ 文件无从下手                                                                                                                                                                                                                                                                                                                                                          |

### P0-3: 添加 pre-commit hooks

| 字段         | 内容                                                                                                                                                                                                                                                                                                                                                                  |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 在提交环节自动拦截 lint/format/type 错误，确保入库代码质量                                                                                                                                                                                                                                                                                                            |
| **涉及文件** | `package.json`（新增 husky/lint-staged 依赖和配置）、`.husky/pre-commit`（新建）                                                                                                                                                                                                                                                                                      |
| **步骤**     | 1. 安装依赖：`npm install -D husky lint-staged`<br>2. 初始化 husky：`npx husky init`<br>3. 编辑 `.husky/pre-commit`：`npx lint-staged`<br>4. 在 `package.json` 中添加 lint-staged 配置：<br>&emsp;`"lint-staged": { "*.{ts,tsx}": ["eslint --fix", "prettier --write"], "*.{json,css,md}": ["prettier --write"] }`<br>5. 测试：故意引入 lint 错误，验证 commit 被阻止 |
| **风险**     | 中低。首次安装后需通知所有开发者执行 `npm install`；若有 CI 环境需确保 husky 在 CI 中被跳过（`HUSKY=0`）                                                                                                                                                                                                                                                              |
| **预期收益** | 从源头阻断格式和类型错误进入仓库，减少 CI 失败率，节省 code review 时间                                                                                                                                                                                                                                                                                               |

### P0-4: 设置测试覆盖率阈值

| 字段         | 内容                                                                                                                                                                                                                                                                               |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 防止覆盖率回退，建立质量底线                                                                                                                                                                                                                                                       |
| **涉及文件** | `vitest.config.ts`、`.github/workflows/ci.yml`                                                                                                                                                                                                                                     |
| **步骤**     | 1. 在 `vitest.config.ts` 中添加 `coverage.threshold` 配置：<br>&emsp;`thresholds: { statements: 60, branches: 50, functions: 60, lines: 60 }`<br>2. 在 CI workflow 中添加 `npm run test:coverage` 步骤（替换或补充现有 `npm test`）<br>3. 运行一次本地验证，确认当前覆盖率满足阈值 |
| **风险**     | 低。若当前覆盖率低于阈值会导致 CI 失败，需先运行 `npm run test:coverage` 确认基线                                                                                                                                                                                                  |
| **预期收益** | 覆盖率有明确底线且受 CI 守护，后续 PR 不会引入无测试的新代码                                                                                                                                                                                                                       |

### P0-5: 创建 typed IPC 通道类型映射

| 字段         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **目标**     | 消除前端全部 `as` 类型断言，建立编译时安全的 IPC 通信协议                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **涉及文件** | `src/types/ipc.ts`（新建）、`src/env.d.ts`（修改）、`src/stores/*.ts`（修改）、`src/api/ipc.ts`（新建）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **步骤**     | 1. 创建 `src/types/ipc.ts`，定义 `IpcChannelMap` 接口，为每个 IPC channel 明确参数和返回值类型：<br>&emsp;`interface IpcChannelMap { 'problems-list': { args: [ProblemFilter?], result: Problem[] }; 'problems-submit': { args: [SubmitPayload], result: SubmitResult }; ... }`<br>2. 创建 `src/api/ipc.ts`，提供类型安全的封装函数：<br>&emsp;`function invoke<K extends keyof IpcChannelMap>(channel: K, ...args: IpcChannelMap[K]['args']): Promise<IpcChannelMap[K]['result']>`<br>3. 更新 `env.d.ts` 中 `invoke` 签名为泛型版本<br>4. 逐个替换各 store 中的 `window.api.invoke(...) as Xxx` 为新的 typed invoke<br>5. 运行 `npm run typecheck` 验证无类型错误 |
| **风险**     | 中。涉及多文件改动，需逐个 store 迁移并验证功能不受影响                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **预期收益** | 编译时即可捕获 IPC 参数/返回值类型不匹配；新 channel 自动获得类型提示；后续可自动生成 IPC 文档                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

### P0-6: 统一重复的类型定义

| 字段         | 内容                                                                                                                                                                                                                                                                                  |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 消除 `AIConfig`、`ProblemRow`、`MistakeRow` 等类型的重复定义，建立单一事实来源                                                                                                                                                                                                        |
| **涉及文件** | `src/types/` 目录（扩充）、`electron/ipc/ai.ts`、`electron/ipc/database.ts`、`electron/ipc/problems.ts`、`src/stores/settingsStore.ts`                                                                                                                                                |
| **步骤**     | 1. 审计所有重复定义：`AIConfig`（3处）、`ProblemRow`/`MistakeRow`（定义在 `problems.ts` 底部）<br>2. 将共享类型统一收入 `src/types/` 目录（前端使用）和 `electron/types/` 目录（如需分离）<br>3. 更新所有引用处 import 统一来源<br>4. 删除旧的内联定义<br>5. `npm run typecheck` 验证 |
| **风险**     | 低。纯重构，类型不变                                                                                                                                                                                                                                                                  |
| **预期收益** | 修改类型定义时只需改一处；减少因多处定义不同步导致的隐蔽 bug                                                                                                                                                                                                                          |

---

## 三、P1 — 近期规划（1-2 周）

### P1-1: 集成 electron-updater 自动更新

| 字段         | 内容                                                                                                                                                                                                                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 用户安装后能自动获取更新，无需手动下载                                                                                                                                                                                                                                                      |
| **涉及文件** | `electron/main.ts`、`electron-builder.yml`、`package.json`                                                                                                                                                                                                                                  |
| **步骤**     | 1. 安装 `electron-updater`<br>2. 在 main 进程中实现 `autoUpdater` 检查逻辑（启动时检查、用户手动检查、下载进度通知）<br>3. 配置 `electron-builder.yml` 的 `publish` 指向 GitHub Releases<br>4. 添加 renderer 侧更新通知 UI（下载进度条、重启提示）<br>5. 测试：本地搭建静态服务器模拟更新源 |
| **风险**     | 中。需要 GitHub Releases 作为发布后端；首次配置需验证签名和更新流程                                                                                                                                                                                                                         |
| **预期收益** | 用户始终运行最新版本，减少因旧版本导致的 bug 报告                                                                                                                                                                                                                                           |

### P1-2: 配置代码签名

| 字段         | 内容                                                                                                                                                                                                                                                                                                                                           |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 消除 Windows SmartScreen / macOS Gatekeeper 安全警告，提升用户信任度                                                                                                                                                                                                                                                                           |
| **涉及文件** | `electron-builder.yml`、CI secrets 配置                                                                                                                                                                                                                                                                                                        |
| **步骤**     | 1. 申请 Windows EV 代码签名证书（或普通 OV 证书作为过渡）<br>2. 配置 `electron-builder.yml`：`win.signingHashAlgorithms: ['sha256']`、`win.certificateFile` / `win.certificateSubjectName`<br>3. macOS：配置 Developer ID 证书 + `notarize` 步骤<br>4. 在 CI 中通过 secrets 注入证书，构建时自动签名<br>5. 验证签名后 exe 不再触发 SmartScreen |
| **风险**     | 中。需要购买证书（Windows EV ~$300/年）；macOS 需要 Apple Developer 账号（$99/年）                                                                                                                                                                                                                                                             |
| **预期收益** | 用户双击安装包时不再看到"未知发布者"警告，大幅提升转化率                                                                                                                                                                                                                                                                                       |

### P1-3: 添加多平台构建支持

| 字段         | 内容                                                                                                                                                                                                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 支持 macOS（dmg）和 Linux（AppImage/deb）构建，兑现 README 的跨平台承诺                                                                                                                                                                                                 |
| **涉及文件** | `electron-builder.yml`、`.github/workflows/`                                                                                                                                                                                                                            |
| **步骤**     | 1. 在 `electron-builder.yml` 中添加 `mac` 和 `linux` target 配置<br>2. 添加 GitHub Actions release workflow：tag 触发 -> matrix 构建三平台 -> 创建 Release -> 上传 artifacts<br>3. 处理平台特异性依赖（`better-sqlite3` 需要原生编译）<br>4. 验证三平台安装包可正常启动 |
| **风险**     | 中。`better-sqlite3` 原生模块在跨平台 CI 构建时需要正确配置；macOS 代码签名需额外步骤                                                                                                                                                                                   |
| **预期收益** | 覆盖三大桌面平台用户群，扩大项目受众                                                                                                                                                                                                                                    |

### P1-4: 添加 Loading 状态组件

| 字段         | 内容                                                                                                                                                                                                            |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 消除列表加载时的空白页面，提供流畅的加载体验                                                                                                                                                                    |
| **涉及文件** | `src/components/`（新增 Skeleton/Spinner）、`src/modules/problems/ProblemList.tsx`、`src/modules/mistakes/MistakesView.tsx`、`src/modules/knowledge/KnowledgeView.tsx`、各相关 store                            |
| **步骤**     | 1. 创建通用 `<Skeleton>` 和 `<Spinner>` 组件<br>2. 在各 store 中添加 `loading` 状态字段<br>3. 在 ProblemList、MistakesView、KnowledgeView 中根据 loading 状态展示 skeleton<br>4. 确保错误状态也有对应的 UI 反馈 |
| **风险**     | 低。纯 UI 增强                                                                                                                                                                                                  |
| **预期收益** | 用户感知加载速度提升，消除"是否卡住了"的疑虑                                                                                                                                                                    |

### P1-5: 引入 zod 做数据库行校验

| 字段         | 内容                                                                                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 消除 IPC/database 层的 `as` 类型断言，建立运行时类型安全                                                                                                                                                                                                                                                       |
| **涉及文件** | `electron/ipc/*.ts`、`package.json`（新增 zod 依赖）、`src/types/`（新增 schema 定义）                                                                                                                                                                                                                         |
| **步骤**     | 1. 安装 `zod`<br>2. 为各数据库表定义 zod schema（`ProblemRowSchema`、`MistakeRowSchema`、`AIConfigSchema` 等）<br>3. 在 IPC handler 中将 `db.prepare().get()` / `.all()` 的返回值通过 schema.parse() 校验<br>4. 将 `as SomeType` 替换为 schema.parse()<br>5. 对校验失败情况做优雅降级（记录日志 + 返回默认值） |
| **风险**     | 低。引入新依赖但 zod 已非常成熟；校验失败时需要有兜底策略                                                                                                                                                                                                                                                      |
| **预期收益** | 数据库 schema 变更时在运行时即可捕获，而非静默产生错误数据                                                                                                                                                                                                                                                     |

### P1-6: 建立 Service 层解耦 IPC handler

| 字段         | 内容                                                                                                                                                                                                                                                                                                              |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 将业务逻辑从 IPC handler 中抽离，实现单一职责，提升可测试性                                                                                                                                                                                                                                                       |
| **涉及文件** | `electron/services/`（新建目录）、`electron/ipc/problems.ts`、`electron/ipc/chat.ts`、`electron/ipc/database.ts`                                                                                                                                                                                                  |
| **步骤**     | 1. 创建 `electron/services/` 目录<br>2. 从 `problems.ts` 的 `problems-submit` handler 中抽取判题逻辑至 `services/problemService.ts`<br>3. 从 `chat.ts` 的 295 行中抽取 AI 对话管理逻辑至 `services/chatService.ts`<br>4. IPC handler 简化为：参数校验 -> 调用 service -> 返回结果<br>5. 为 service 层编写单元测试 |
| **风险**     | 中。涉及较大的代码重组，需逐个 handler 迁移并验证功能完整                                                                                                                                                                                                                                                         |
| **预期收益** | IPC handler 代码量减少 60%+；业务逻辑可独立测试；新增功能只需编写 service + 注册 handler                                                                                                                                                                                                                          |

### P1-7: 创建 IPC API 文档

| 字段         | 内容                                                                                                                                                                                                                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 为 30+ 个 IPC channel 提供完整的人类可读文档                                                                                                                                                                                                                                                |
| **涉及文件** | `docs/ipc-api.md`（新建）                                                                                                                                                                                                                                                                   |
| **步骤**     | 1. 遍历所有 `ipcMain.handle` 注册点，提取 channel 名称、参数类型、返回值类型、错误处理方式<br>2. 按功能域分类（题目、AI、知识库、代码运行、设置等）<br>3. 为每个 channel 编写文档：用途、请求参数、响应格式、错误码、示例<br>4. 如果 P0-5 完成了 typed IPC，可基于 `IpcChannelMap` 自动生成 |
| **风险**     | 低。纯文档                                                                                                                                                                                                                                                                                  |
| **预期收益** | 贡献者可快速理解 IPC 协议，无需逐个阅读 handler 源码                                                                                                                                                                                                                                        |

### P1-8: 添加 LIKE 通配符转义

| 字段         | 内容                                                                                                                                                      |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 修复知识库搜索中 `%` 和 `_` 通配符导致的非预期匹配问题                                                                                                    |
| **涉及文件** | `electron/ipc/rag.ts`                                                                                                                                     |
| **步骤**     | 1. 在 `rag.ts` 中为搜索关键词添加 `escapeLike()` 函数，转义 `%`、`_` 和 `[` 字符<br>2. 将 `%${kw}%` 替换为 `%${escapeLike(kw)}%`<br>3. 添加对应的单元测试 |
| **风险**     | 低。改动范围小且明确                                                                                                                                      |
| **预期收益** | 搜索结果更精确，消除用户输入特殊字符时的异常行为                                                                                                          |

---

## 四、P2 — 中期规划（1-2 月）

### P2-1: 添加 React 组件测试

| 字段         | 内容                                                                                                                                                                                                                                                                                                        |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 覆盖核心 React 组件的渲染和交互逻辑                                                                                                                                                                                                                                                                         |
| **涉及文件** | `tests/components/`（新建）、`package.json`（新增 @testing-library/react、jsdom 依赖）、`vitest.config.ts`                                                                                                                                                                                                  |
| **步骤**     | 1. 安装 `@testing-library/react`、`@testing-library/jest-dom`、`jsdom`<br>2. 配置 vitest 的 jsdom 环境<br>3. 优先测试核心组件：`ChatView`、`ProblemList`、`ProblemDetail`、`SettingsView`<br>4. Mock IPC 层（`window.api.invoke`）<br>5. 测试场景：加载状态、空状态、错误状态、用户交互（提交、切换、删除） |
| **风险**     | 低。新增测试不会影响现有功能                                                                                                                                                                                                                                                                                |
| **预期收益** | 组件回归有保障；重构 UI 时有信心不破坏功能                                                                                                                                                                                                                                                                  |

### P2-2: 添加 Zustand Store 测试

| 字段         | 内容                                                                                                                                                                                  |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 验证 store 状态逻辑的正确性                                                                                                                                                           |
| **涉及文件** | `tests/stores/`（新建）                                                                                                                                                               |
| **步骤**     | 1. 为每个 store（appStore、editorStore、problemStore、chatStore、settingsStore）编写测试<br>2. Mock `window.api.invoke` 返回值<br>3. 测试：状态更新、异步操作、错误处理、store 间交互 |
| **风险**     | 低                                                                                                                                                                                    |
| **预期收益** | 状态管理逻辑有测试保障                                                                                                                                                                |

### P2-3: 添加关键路径 E2E 测试

| 字段         | 内容                                                                                                                                                                                                                                                                                 |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **目标**     | 覆盖核心用户流程的端到端验证                                                                                                                                                                                                                                                         |
| **涉及文件** | `tests/e2e/`（新建）、`package.json`（新增 Playwright 依赖）                                                                                                                                                                                                                         |
| **步骤**     | 1. 安装 `@playwright/test` 和 `electron-playwright`<br>2. 编写核心场景测试：<br>&emsp;- 打开应用 -> 浏览题目列表 -> 选择题目 -> 编写代码 -> 提交 -> 查看结果<br>&emsp;- 打开 AI 对话 -> 发送消息 -> 接收流式回复<br>&emsp;- 切换主题 -> 验证 UI 变化<br>3. 在 CI 中添加 E2E 测试步骤 |
| **风险**     | 中。Electron E2E 测试环境配置较复杂；CI 中可能需要虚拟显示（Xvfb）                                                                                                                                                                                                                   |
| **预期收益** | 端到端流程有自动化验证，发布前信心大幅提升                                                                                                                                                                                                                                           |

### P2-4: 添加 a11y 无障碍支持

| 字段         | 内容                                                                                                                                                                                                                                                                                                                      |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 基本无障碍合规，支持键盘导航和屏幕阅读器                                                                                                                                                                                                                                                                                  |
| **涉及文件** | `eslint.config.mjs`（新增 jsx-a11y 插件）、`src/components/Sidebar.tsx`、`src/modules/` 各组件、`src/components/Layout.tsx`                                                                                                                                                                                               |
| **步骤**     | 1. 安装 `eslint-plugin-jsx-a11y` 并配置到 ESLint<br>2. 运行 lint 获取所有 a11y 警告清单<br>3. 系统性修复：<br>&emsp;- 为所有交互元素添加 `aria-label`<br>&emsp;- 为表单输入关联 `<label>` 元素<br>&emsp;- 确保 Tab 顺序合理<br>&emsp;- 验证颜色对比度（使用 axe DevTools）<br>4. 添加 `axe-core` 到组件测试中做自动化检查 |
| **风险**     | 低。纯增量改进                                                                                                                                                                                                                                                                                                            |
| **预期收益** | 支持键盘驱动操作和辅助技术，扩大用户覆盖面                                                                                                                                                                                                                                                                                |

### P2-5: 数据库迁移版本管理

| 字段         | 内容                                                                                                                                                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 用 schema 版本号管理迁移历史，替代当前脆弱的 `PRAGMA table_info` 方式                                                                                                                                                           |
| **涉及文件** | `electron/db/index.ts`、`electron/db/schema.sql`                                                                                                                                                                                |
| **步骤**     | 1. 使用 SQLite 的 `PRAGMA user_version` 追踪 schema 版本<br>2. 将 `ensureSchemaColumns()` 改造为基于版本号的迁移链<br>3. 每次 schema 变更递增版本号，编写对应的 up/down 迁移脚本<br>4. 启动时根据 `user_version` 执行必要的迁移 |
| **风险**     | 中。需要兼容现有数据库（无 user_version 的老数据库），需写一次性迁移逻辑                                                                                                                                                        |
| **预期收益** | Schema 变更有版本追踪，可回滚，多人协作不冲突                                                                                                                                                                                   |

### P2-6: 添加 `npm audit` 到 CI

| 字段         | 内容                                                                                                                                                                                             |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **目标**     | 在 CI 中自动检测已知漏洞依赖                                                                                                                                                                     |
| **涉及文件** | `.github/workflows/ci.yml`                                                                                                                                                                       |
| **步骤**     | 1. 在 CI workflow 中添加步骤：`npm audit --audit-level=high --omit=dev`<br>2. 设置为 warning 而非 block（避免第三方依赖的漏洞阻塞开发）<br>3. 可选：集成 Socket 或 Snyk 做更深层的供应链安全分析 |
| **风险**     | 低。第三方依赖的已知漏洞可能暂时无法修复，需设置为非阻断                                                                                                                                         |
| **预期收益** | 及时发现并修复依赖漏洞                                                                                                                                                                           |

### P2-7: 创建架构决策记录（ADR）

| 字段         | 内容                                                                                                                                                                                                                                                                                                                     |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **目标**     | 记录关键技术选型的决策依据，帮助贡献者理解"为什么"                                                                                                                                                                                                                                                                       |
| **涉及文件** | `docs/adr/`（新建目录）                                                                                                                                                                                                                                                                                                  |
| **步骤**     | 1. 创建 `docs/adr/` 目录和 ADR 模板<br>2. 为以下已做出的决策补充 ADR：<br>&emsp;- Zustand vs Redux<br>&emsp;- 关键词搜索 vs 向量数据库（RAG 方案选型）<br>&emsp;- 进程内代码执行 vs Docker 沙箱<br>&emsp;- better-sqlite3 vs 其他 SQLite 驱动<br>&emsp;- electron-vite vs 其他构建工具<br>3. 未来重大决策按 ADR 流程记录 |
| **风险**     | 低。纯文档                                                                                                                                                                                                                                                                                                               |
| **预期收益** | 贡献者理解技术选型背景，减少重复讨论，新人快速建立架构心智模型                                                                                                                                                                                                                                                           |

### P2-8: 添加 Release 自动化工作流

| 字段         | 内容                                                                                                                                                                                                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**     | 版本发布从手动操作变为 tag 触发的自动化流程                                                                                                                                                                                                                                     |
| **涉及文件** | `.github/workflows/release.yml`（新建）、`electron-builder.yml`                                                                                                                                                                                                                 |
| **步骤**     | 1. 创建 release workflow：<br>&emsp;- 触发条件：推送 `v*` tag<br>&emsp;- 步骤：lint -> test -> build (matrix: win/mac/linux) -> create GitHub Release -> upload artifacts<br>2. 配置 `electron-builder.yml` 的 `publish` 指向 GitHub Releases<br>3. 可选：自动更新 CHANGELOG.md |
| **风险**     | 中。需要正确配置跨平台构建和签名                                                                                                                                                                                                                                                |
| **预期收益** | 一条命令完成全平台发布，减少人为失误                                                                                                                                                                                                                                            |

---

## 五、实施优先级总览

```
┌─────────────────────────────────────────────────────────┐
│  P0 — 立即实施（本轮可完成）                               │
│  ├─ P0-1  补充截图并完善 README                          │
│  ├─ P0-2  提取 CONTRIBUTING.md                           │
│  ├─ P0-3  添加 pre-commit hooks (husky + lint-staged)    │
│  ├─ P0-4  设置测试覆盖率阈值                              │
│  ├─ P0-5  创建 typed IPC 通道类型映射                     │
│  └─ P0-6  统一重复的类型定义                              │
├─────────────────────────────────────────────────────────┤
│  P1 — 近期规划（1-2 周）                                  │
│  ├─ P1-1  集成 electron-updater 自动更新                  │
│  ├─ P1-2  配置代码签名                                    │
│  ├─ P1-3  添加多平台构建支持                              │
│  ├─ P1-4  添加 Loading 状态组件                           │
│  ├─ P1-5  引入 zod 做数据库行校验                         │
│  ├─ P1-6  建立 Service 层解耦 IPC handler                │
│  ├─ P1-7  创建 IPC API 文档                              │
│  └─ P1-8  添加 LIKE 通配符转义                            │
├─────────────────────────────────────────────────────────┤
│  P2 — 中期规划（1-2 月）                                  │
│  ├─ P2-1  添加 React 组件测试                             │
│  ├─ P2-2  添加 Zustand Store 测试                        │
│  ├─ P2-3  添加关键路径 E2E 测试                           │
│  ├─ P2-4  添加 a11y 无障碍支持                            │
│  ├─ P2-5  数据库迁移版本管理                              │
│  ├─ P2-6  添加 npm audit 到 CI                           │
│  ├─ P2-7  创建架构决策记录 (ADR)                          │
│  └─ P2-8  添加 Release 自动化工作流                       │
└─────────────────────────────────────────────────────────┘
```

---

## 六、预期效果

完成 P0 后：

- 综合评分预计从 7.0 提升至 **7.5**
- Build/Release 从 5/10 提升至 6/10（pre-commit + coverage threshold）
- DX 从 7/10 提升至 8/10（CONTRIBUTING.md + pre-commit）
- 文档从 8/10 提升至 **9/10**（截图 + 贡献指南）
- 代码质量从 7/10 提升至 **8/10**（typed IPC + 统一类型）

完成 P1 后：

- 综合评分预计提升至 **8.2**
- Build/Release 从 6/10 提升至 **8/10**（auto-update + 签名 + 多平台）
- 安全保持 **9/10**（LIKE 转义 + zod 校验）

完成 P2 后：

- 综合评分预计达到 **8.5+**
- 测试从 7/10 提升至 **8/10**（组件测试 + E2E）
- UX 从 7/10 提升至 **8/10**（a11y + Loading 状态）
- 架构从 7/10 提升至 **8/10**（Service 层 + 迁移版本管理）
